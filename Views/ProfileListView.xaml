<UserControl x:Class="FFXIManager.Views.ProfileListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:FFXIManager.Converters">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <!-- Note: Other converters are now registered globally in App.xaml -->
    </UserControl.Resources>
    
    <GroupBox Header="Backup Profiles">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Directory Settings - Collapsible -->
            <Expander Grid.Row="0" Header="Directory Settings" IsExpanded="False" Margin="5,5,5,0">
                <StackPanel Orientation="Vertical" Margin="5">
                    <Label Content="PlayOnline Directory:"/>
                    <DockPanel>
                        <Button DockPanel.Dock="Right"
                                Content="📁"
                                Style="{StaticResource CircularInfoButton}"
                                Command="{Binding ChangeDirectoryCommand}"
                                ToolTip="Browse Directory"
                                AutomationProperties.Name="Browse Directory"/>
                        <TextBox Text="{Binding PlayOnlineDirectory}" IsReadOnly="True"/>
                    </DockPanel>

                    <CheckBox Content="Show auto-backups"
                              IsChecked="{Binding ShowAutoBackups}"
                              VerticalAlignment="Center"
                              Margin="0,5,0,0"/>
                </StackPanel>
            </Expander>

            <!-- Toolbar -->
            <Border Grid.Row="1"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,1,0,1"
                    Margin="5,5,5,0">
                <DockPanel Margin="8,4">
                    <Button DockPanel.Dock="Left"
                            Content="➕"
                            Style="{StaticResource CircularPrimaryButton}"
                            Command="{Binding ShowAddProfileDialogCommand}"
                            ToolTip="Add Profile"
                            AutomationProperties.Name="Add Profile"/>
                    <Button DockPanel.Dock="Right"
                            Content="🔄"
                            Style="{StaticResource CircularSuccessButton}"
                            Command="{Binding RefreshCommand}"
                            ToolTip="Refresh"
                            AutomationProperties.Name="Refresh Profiles"/>
                    <Border/> <!-- Spacer -->
                </DockPanel>
            </Border>

            <!-- Profiles DataGrid -->
            <DataGrid Grid.Row="2" 
                      Style="{StaticResource DataGridBase}"
                      ItemsSource="{Binding Profiles}" 
                      SelectedItem="{Binding SelectedProfile}"
                      AutoGenerateColumns="False"
                      SelectionMode="Single"
                      IsReadOnly="True"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.ContextMenu>
                    <ContextMenu Background="{DynamicResource SurfaceBrush}"
                               BorderBrush="{DynamicResource BorderBrush}"
                               Foreground="{DynamicResource PrimaryTextBrush}">
                        <MenuItem Header="🔄 Swap to This Profile"
                                  Command="{Binding SwapProfileCommand}"
                                  FontWeight="Bold"
                                  Background="{DynamicResource SurfaceBrush}"
                                  Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <Separator Background="{DynamicResource DividerBrush}"/>
                        <MenuItem Header="✏️ Rename Profile" 
                                  Command="{Binding RenameProfileCommand}"
                                  Background="{DynamicResource SurfaceBrush}"
                                  Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <MenuItem Header="🗑️ Delete Profile" 
                                  Command="{Binding DeleteProfileCommand}"
                                  Background="{DynamicResource SurfaceBrush}"
                                  Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <Separator Background="{DynamicResource DividerBrush}"/>
                        <MenuItem Header="📋 Copy Profile Name" 
                                  Command="{Binding CopyProfileNameCommand}"
                                  Background="{DynamicResource SurfaceBrush}"
                                  Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <MenuItem Header="📁 Open File Location"
                                  Command="{Binding OpenFileLocationCommand}"
                                  Background="{DynamicResource SurfaceBrush}"
                                  Foreground="{DynamicResource PrimaryTextBrush}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Profile Name" Binding="{Binding Name}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridTextCellStyle}"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="File Size" Binding="{Binding FileSizeFormatted, Mode=OneWay}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridTextCellStyle}"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Last Modified" Binding="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridTextCellStyle}"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock FontWeight="{Binding IsCurrentlyActive, Mode=OneWay, Converter={StaticResource BoolToFontWeightConverter}}"
                                           Foreground="{Binding IsCurrentlyActive, Mode=OneWay, ConverterParameter=Status, Converter={StaticResource BoolToColorConverter}}">
                                    <TextBlock.Text>
                                        <MultiBinding Converter="{StaticResource ProfileStatusConverter}">
                                            <Binding Path="IsSystemFile" Mode="OneWay"/>
                                            <Binding Path="IsLastUserChoice" Mode="OneWay"/>
                                            <Binding Path="IsCurrentlyActive" Mode="OneWay"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Actions" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="⋯"
                                        Style="{StaticResource CircularSmallButton}"
                                        Background="{DynamicResource InfoBrush}"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        ToolTip="Profile Actions"
                                        AutomationProperties.Name="Profile Actions"
                                        Visibility="{Binding IsSystemFile, Mode=OneWay, Converter={StaticResource InverseBoolToVisConverter}}"
                                        Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Click="ActionsButton_Click">
                                    <Button.ContextMenu>
                                        <ContextMenu Background="{DynamicResource SurfaceBrush}"
                                                   BorderBrush="{DynamicResource BorderBrush}"
                                                   Foreground="{DynamicResource PrimaryTextBrush}">
                                            <MenuItem Header="🔄 Swap to This Profile"
                                                      Command="{Binding Path=PlacementTarget.Tag.SwapProfileParameterCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding}"
                                                      FontWeight="Bold"
                                                      Background="{DynamicResource SurfaceBrush}"
                                                      Foreground="{DynamicResource PrimaryTextBrush}"/>
                                            <Separator Background="{DynamicResource DividerBrush}"/>
                                            <MenuItem Header="✏️ Rename Profile"
                                                      Command="{Binding Path=PlacementTarget.Tag.RenameProfileParameterCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding}"
                                                      Background="{DynamicResource SurfaceBrush}"
                                                      Foreground="{DynamicResource PrimaryTextBrush}"/>
                                            <MenuItem Header="🗑️ Delete Profile"
                                                      Command="{Binding Path=PlacementTarget.Tag.DeleteProfileParameterCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding}"
                                                      Background="{DynamicResource SurfaceBrush}"
                                                      Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </GroupBox>
</UserControl>