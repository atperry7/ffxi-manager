<Window x:Class="FFXIManager.Views.RenameProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Profile" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="?? Rename Profile" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="1" Text="Current name:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="2" x:Name="CurrentNameTextBlock" Text="{Binding CurrentName}" 
                   Foreground="Gray" Margin="0,0,0,10"/>
        
        <StackPanel Grid.Row="3">
            <TextBlock Text="New name:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="NewNameTextBox" Text="{Binding NewProfileName, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,10"/>
            <TextBlock Text="?? Tip: This will convert auto-backups (backup_*) to user profiles" 
                       Foreground="Gray" FontSize="11" TextWrapping="Wrap"/>
        </StackPanel>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Rename" Width="80" Click="Rename_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>