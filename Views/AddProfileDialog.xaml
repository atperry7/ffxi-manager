<Window x:Class="FFXIManager.Views.AddProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Profile" Height="220" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Create New Backup Profile" FontWeight="Bold" FontSize="16" Margin="0,0,0,12"/>
        <Label Grid.Row="1" Content="Backup Name:"/>
        <TextBox Grid.Row="2" x:Name="BackupNameTextBox" Margin="0,4,0,0" Height="28"
                 Text="{Binding NewBackupName, UpdateSourceTrigger=PropertyChanged}"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Create" Width="90" Margin="0,0,8,0" IsDefault="True"
                    Command="{Binding CreateBackupCommand}"/>
            <Button Content="Cancel" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
